[project]
name = "halstead-complexity"
version = "0.1.0"
description = "Measure Halstead code complexity"
readme = "README.md"
license = "MIT"
authors = [
    { name = "EpicAlbin03" }
]
keywords = ["halstead", "code complexity"]
requires-python = ">=3.12"
dependencies = [
    "platformdirs>=4.4.0",
    "tree-sitter>=0.25.2",
    "tree-sitter-python>=0.23.8",
    "tree-sitter-javascript>=0.23.5",
    "typer>=0.19.2",
    "dynaconf>=3.2.11",
]

[project.scripts]
hc = "halstead_complexity.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
